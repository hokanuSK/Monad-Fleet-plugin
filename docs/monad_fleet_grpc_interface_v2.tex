\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage[hidelinks]{hyperref}
\usepackage{enumitem}
\usepackage{longtable}

\setlist{noitemsep,topsep=4pt}

\title{Monad Fleet gRPC Interface Specification (fleet.v2)\\Implementation Reference for Wi-Fi Devices}
\author{FleetManager Project}
\date{\today}

\begin{document}
\maketitle

\section{Purpose and Scope}
This document specifies the currently implemented gRPC interface for device agents that communicate with Monad Fleet service.
The goal is interoperability: any device capable of Wi-Fi networking can implement this client contract and participate in fleet experiments.

This specification is implementation-based, aligned with the running code and protobuf files in this repository.
It also documents the current device-side data persistence model (local spool with deferred upload).
\textbf{Important:} the gRPC contract is backend-agnostic. The current deployment maps runs into eLabFTW (items/resources + scheduler events) and forwards numeric metrics into a Prometheus-compatible pipeline, but agents do not depend on eLabFTW-specific concepts.

\section{Source of Truth}
\begin{itemize}
  \item Protobuf contract (v2): \texttt{monad-fleet-service/proto/fleet\_gateway\_v2.proto}
  \item Reference client (v2): \texttt{device-sim/agent\_v2\_client.py}
  \item Server behavior: \texttt{monad-fleet-service/gateway\_server.py}
  \item Legacy API (v1, still served): \texttt{monad-fleet-service/proto/fleet\_gateway.proto}
\end{itemize}

\section{System Context}
\begin{itemize}
  \item \textbf{Agent}: software running on a measurement device (for example Raspberry Pi).
  \item \textbf{Monad Fleet service}: gRPC server, policy resolver, and report ingestor.
  \item \textbf{eLabFTW}: experiment registry and scheduler/event storage backend used by Monad Fleet.
  \item \textbf{Metrics backend (optional)}: Prometheus scrape + remote\_write into Mimir and visualization in Grafana.
\end{itemize}

Communication path:
\begin{verbatim}
Device Agent <-> gRPC (fleet.v2) <-> Monad Fleet <-> eLabFTW REST API
\end{verbatim}

\section{Transport and Endpoint}
\begin{itemize}
  \item Protocol: gRPC over HTTP/2.
  \item Default port: \texttt{50060}.
  \item Default deployment endpoint in this project: \texttt{<host>:50060}.
  \item Current reference client uses \texttt{grpc.insecure\_channel()} (no TLS).
  \item HTTP sidecar for observability and lightweight ingest: \texttt{<host>:9108}.
  \item HTTP endpoints: \texttt{GET /metrics}, \texttt{POST /ingest/v1/metrics}, \texttt{GET /healthz}.
\end{itemize}

\textbf{Recommendation:} run on trusted LAN/VPN, or add mTLS in a future hardening step.

\section{Contract Principles}
\subsection{Backend independence}
Agents only talk to a Fleet Manager endpoint over gRPC (and optionally HTTP for lightweight metrics ingest).
How the Fleet Manager persists and routes data (eLabFTW, files, object storage, time-series databases) is a deployment concern and must not leak into the agent contract.

\subsection{Forward compatibility}
\begin{itemize}
  \item Agents must ignore unknown fields and enum values.
  \item Servers must accept requests that omit optional fields.
  \item String-to-string maps (\texttt{Event.metrics}, \texttt{Report.summary\_metrics}) are treated as open extension points.
\end{itemize}

\subsection{Data routing and ``send anywhere''}
The ``send data'' operation in this protocol is \texttt{PublishReport} (and optionally \texttt{PublishEvents}).
Reports carry:
\begin{itemize}
  \item structured events (typed, timestamped),
  \item artifact references (URIs/keys, checksums),
  \item summary metrics (key/value).
\end{itemize}
This is intentionally generic: devices always send to Fleet Manager, while Fleet Manager can be configured to forward data to any storage or observability system (e.g., Prometheus remote\_write to Mimir, or a future OpenTelemetry exporter).

\section{Service Definition}
The served package is \texttt{fleet.v2} with service \texttt{FleetManager}.

\begin{verbatim}
rpc Hello(HelloRequest) returns (HelloResponse);
rpc GetAssignment(GetAssignmentRequest) returns (GetAssignmentResponse);
rpc GetPolicy(GetPolicyRequest) returns (GetPolicyResponse);
rpc AckPrepared(AckPreparedRequest) returns (AckPreparedResponse);
rpc PublishReport(PublishReportRequest) returns (PublishReportResponse);
rpc PublishEvents(PublishEventsRequest) returns (PublishEventsResponse); // optional
\end{verbatim}

\section{Operational Model}
\subsection{Phases}
\begin{itemize}
  \item \textbf{PREPARE}: \texttt{Hello}, \texttt{GetAssignment}, \texttt{GetPolicy}, \texttt{AckPrepared}
  \item \textbf{MEASURE}: local command execution on device
  \item \textbf{REPORT}: \texttt{PublishReport} (and optional \texttt{PublishEvents})
\end{itemize}

\subsection{Default Polling}
Server returns \texttt{recommended\_prepare\_poll\_sec} in \texttt{HelloResponse}. Agents should poll using this value.

\section{Control Plane Modes}
\begin{itemize}
  \item \texttt{RF\_SHARING}: expected for Wi-Fi-only devices.
  \item \texttt{DUAL\_NIC}: allowed only if device advertises Ethernet, and if \texttt{control\_plane\_iface} is provided it must be Ethernet-like (\texttt{eth*}).
\end{itemize}

Implemented server behavior:
\begin{itemize}
  \item If agent requests \texttt{DUAL\_NIC} without Ethernet interface, server downgrades to \texttt{RF\_SHARING}.
  \item \texttt{AckPrepared} rejects \texttt{DUAL\_NIC} when \texttt{route\_verified=false} or when \texttt{control\_plane\_iface} is provided and is not Ethernet-like.
\end{itemize}

\section{RPC Semantics}
\subsection{\texttt{Hello}}
\textbf{Required input}: \texttt{agent.agent\_id}.

\textbf{Purpose}:
\begin{itemize}
  \item register/refresh device presence,
  \item update metadata (hostname, interfaces, capabilities, selected mode),
  \item negotiate allowed control-plane mode.
\end{itemize}

\textbf{Important response fields}:
\begin{itemize}
  \item \texttt{server\_version} (informational)
  \item \texttt{server\_time} (informational)
  \item \texttt{allowed\_mode}
  \item \texttt{mode\_reason}
  \item \texttt{recommended\_prepare\_poll\_sec}
  \item \texttt{max\_batch\_size} (informational; reserved for future batching)
\end{itemize}

\subsection{\texttt{GetAssignment}}
\textbf{Required input}: \texttt{agent\_id}.

Response:
\begin{itemize}
  \item \texttt{ASSIGNED}: returns \texttt{experiment\_id}, \texttt{policy\_id}, measurement window.
  \item \texttt{NO\_WORK}: no matching policy currently available.
\end{itemize}

\subsection{\texttt{GetPolicy}}
\textbf{Required input}: \texttt{agent\_id}. Optional: \texttt{experiment\_id}, \texttt{last\_policy\_id}.

Response:
\begin{itemize}
  \item \texttt{OK}: full \texttt{Policy}.
  \item \texttt{NOT\_MODIFIED}: same revision as \texttt{last\_policy\_id}.
  \item \texttt{NO\_POLICY}: no policy for this device or experiment mismatch.
\end{itemize}

\subsection{\texttt{AckPrepared}}
\textbf{Required input}: \texttt{agent\_id}, \texttt{preparation\_id}, \texttt{policy\_id}.

Validation behavior:
\begin{itemize}
  \item rejects if no policy currently assigned to device,
  \item rejects on policy revision mismatch,
  \item for \texttt{DUAL\_NIC}, rejects if route/interface requirements fail.
\end{itemize}

Idempotency:
\begin{itemize}
  \item duplicate \texttt{preparation\_id} is accepted and treated as already acknowledged.
\end{itemize}

\subsection{\texttt{PublishReport}}
\textbf{Required input}: \texttt{agent\_id} (or \texttt{report.agent\_id}) and \texttt{report.run\_id}.

Behavior:
\begin{itemize}
  \item converts v2 report/events to internal v1 event model,
  \item appends/updates eLabFTW scheduler event metadata,
  \item stores artifact references as \texttt{ARTIFACT\_UPLOADED} events.
\end{itemize}

Status:
\begin{itemize}
  \item \texttt{ACCEPTED}
  \item \texttt{DUPLICATE} (same \texttt{run\_id|agent\_id|policy\_id})
  \item \texttt{REJECTED}
\end{itemize}

\subsection{\texttt{PublishEvents}}
\textbf{Default in this deployment}: disabled (\texttt{ALLOW\_LIVE\_EVENTS=false}).

When disabled, every ack is \texttt{REJECTED} with reason \texttt{live events disabled}.

\section{Data Model Reference}
\subsection{Common conventions}
\begin{itemize}
  \item All timestamps are UTC (\texttt{google.protobuf.Timestamp} on the wire; ISO-8601 with \texttt{Z} in JSON policy).
  \item All IDs are opaque strings. Recommended formats:
    \begin{itemize}
      \item \texttt{agent\_id}: stable MAC (lower-case) or UUID.
      \item \texttt{run\_id}: UUIDv4.
      \item \texttt{event\_id}: UUIDv4 or deterministic hash; should be unique for de-duplication (server can synthesize if omitted).
      \item \texttt{policy\_id}: immutable revision identifier (server computes \texttt{sha256:<digest>}).
    \end{itemize}
  \item Numeric metrics are transported as strings in maps; servers parse float. Non-numeric values are ignored for time-series export.
  \item \texttt{Report.summary\_metrics} and \texttt{Event.metrics} are open extension points: devices may add keys without breaking compatibility.
\end{itemize}

\subsection{\texttt{AgentDescriptor}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{agent\_id} & yes & Stable unique identifier of the device/agent. Recommended: lower-case MAC address (\texttt{aa:bb:cc:dd:ee:ff}) or UUID. \\
\texttt{hostname} & no & Human-friendly identifier for debugging and inventory. \\
\texttt{agent\_version} & no & Agent software version string (semantic version recommended). \\
\texttt{interfaces} & no & List of known network interfaces; used for control-plane mode negotiation and auditing. \\
\texttt{requested\_mode} & no & Requested control-plane isolation mode (\texttt{RF\_SHARING} vs \texttt{DUAL\_NIC}). \\
\texttt{control\_plane\_iface} & no & Interface name to be used for control-plane traffic (\texttt{wlan0}, \texttt{eth0}). \\
\texttt{fleet\_manager\_target} & no & Fleet Manager host:port that agent uses for route verification (informational). \\
\texttt{capabilities} & no & Free-form capability tokens (e.g., \texttt{csi}, \texttt{ble}, \texttt{wifi}). \\
\hline
\end{longtable}

\subsection{\texttt{Policy}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{experiment\_id} & yes & Global experiment identifier (in current eLabFTW profile: \texttt{elabftw:<id>}). \\
\texttt{policy\_id} & yes & Immutable revision identifier of the plan. Agents must treat it as opaque and use it for caching and idempotency keys. \\
\texttt{issued\_at} & no & Time when Fleet Manager issued the policy response. \\
\texttt{measurement\_from/to} & no & Overall time window (agent may refuse to execute outside the window). \\
\texttt{command\_groups} & yes & Ordered groups; each group contains ordered commands. \\
\texttt{allowed\_mode} & no & Effective allowed mode after server authorization. \\
\texttt{control\_plane\_iface} & no & Effective control-plane interface when \texttt{allowed\_mode=DUAL\_NIC}. \\
\hline
\end{longtable}

\subsection{\texttt{CommandGroup} and \texttt{Command}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{CommandGroup.id} & yes & Stable identifier within the policy (used for correlation in events). \\
\texttt{CommandGroup.name} & no & Human-friendly name. \\
\texttt{CommandGroup.from/to} & no & Optional per-group time window; groups outside the window may be skipped. \\
\texttt{CommandGroup.failure\_mode} & no & \texttt{FAIL\_FAST} stops the group after first error; \texttt{CONTINUE\_ON\_ERROR} continues. \\
\texttt{CommandGroup.commands} & yes & Ordered commands to execute within this group. \\
\texttt{Command.id} & yes & Stable identifier within the group (used for correlation in events). \\
\texttt{Command.type} & yes & One of \texttt{SHELL}, \texttt{WIFI\_SCAN}, \texttt{BLE\_SCAN}, \texttt{CAPTURE\_CSI}. \\
\texttt{Command.cmdline} & no & Shell command line for \texttt{SHELL}. For typed commands, this is optional and may be omitted; agents may treat it as an override/hint. \\
\texttt{Command.argv} & no & Optional argv array; future-proof alternative to \texttt{cmdline}. \\
\texttt{Command.env} & no & Environment variables for execution. \\
\texttt{Command.timeout\_ms} & no & Per-command timeout in milliseconds. \\
\texttt{Command.retry} & no & Retry policy (\texttt{max\_attempts}, \texttt{backoff\_ms}). \\
\texttt{Command.expected\_artifacts} & no & Optional artifact expectations (naming rules, globs, mime). \\
\hline
\end{longtable}

\subsection{\texttt{RetryPolicy}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{max\_attempts} & no & Total attempts including the first attempt (must be $\ge$ 1 to retry). \\
\texttt{backoff\_ms} & no & Milliseconds to wait between attempts (simple fixed backoff). \\
\hline
\end{longtable}

\subsection{\texttt{ArtifactSpec}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{name} & yes & Artifact label (used for correlation and naming conventions). \\
\texttt{path\_or\_glob} & no & Expected path or glob pattern on device (implementation-specific). \\
\texttt{mime} & no & MIME type hint (informational). \\
\hline
\end{longtable}

\subsection{\texttt{Report} and \texttt{Event}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{Report.run\_id} & yes & Unique identifier of one run of one policy on one agent. Used for de-duplication. \\
\texttt{Report.experiment\_id} & yes & Experiment identifier for this run. Used for storage/mapping and debugging. \\
\texttt{Report.policy\_id} & yes & Policy revision identifier for this run. Used for idempotency and mapping. \\
\texttt{Report.agent\_id} & no & Agent identifier (may be provided via request \texttt{agent\_id} field instead). \\
\texttt{Report.started\_at} & no & Run start time (informational). \\
\texttt{Report.finished\_at} & no & Run finish time (informational; used for summary synthesis if \texttt{events} is empty). \\
\texttt{Report.status} & yes & Human-readable final status (\texttt{OK}/\texttt{FAILED} in current reference agent). \\
\texttt{Report.events} & no & Structured event list. If empty, server may synthesize a summary event. \\
\texttt{Report.artifacts} & no & Artifact references only; blobs are not transported over gRPC. \\
\texttt{Report.summary\_metrics} & no & Free-form summary metrics (strings). Numeric keys are exported to time-series backend. \\
\texttt{Event.event\_id} & no & Unique event id for de-duplication. Recommended to set; server will synthesize if missing. \\
\texttt{Event.run\_id} & no & Run identifier (server uses report/run fallbacks if missing). \\
\texttt{Event.experiment\_id} & no & Experiment identifier (server uses report fallbacks if missing). \\
\texttt{Event.policy\_id} & no & Policy revision identifier (server uses report fallbacks if missing). \\
\texttt{Event.agent\_id} & no & Agent identifier (server uses request/report fallbacks if missing). \\
\texttt{Event.timestamp} & no & Event time (used for ordering and synthetic ids). \\
\texttt{Event.type} & yes & Structured event type (run start/finish, command start/finish, artifact upload, error). \\
\texttt{Event.group\_id} & no & Command group correlation id. \\
\texttt{Event.command\_id} & no & Command correlation id. \\
\texttt{Event.exit\_code} & no & Exit code for command events (0 for success). \\
\texttt{Event.duration\_ms} & no & Duration for command events. \\
\texttt{Event.message} & no & Short message (used as stdout tail in v1 mapping). \\
\texttt{Event.metrics} & no & Free-form metrics for this event. Numeric values may be exported to time-series backend. \\
\hline
\end{longtable}

\subsection{\texttt{ArtifactRef}}
\begin{longtable}{p{4.2cm}p{1.6cm}p{9.2cm}}
\textbf{Field} & \textbf{Req} & \textbf{Semantics} \\
\hline
\endhead
\texttt{name} & yes & Human-friendly artifact name (also used as command id in the eLabFTW mapping). \\
\texttt{uri} & yes & Reference to the artifact location (e.g., \texttt{spool://...}, \texttt{s3://...}, \texttt{https://...}). \\
\texttt{size\_bytes} & no & Size for auditing. \\
\texttt{sha256} & no & SHA-256 checksum for integrity checking. \\
\hline
\end{longtable}

\section{Idempotency and De-duplication}
\begin{itemize}
  \item Event dedupe key: \texttt{event\_id}
  \item Prepare dedupe key: \texttt{preparation\_id}
  \item Report dedupe key: \texttt{run\_id|agent\_id|policy\_id}
\end{itemize}

Server state is persisted in:
\begin{itemize}
  \item \texttt{/data/state.json} (container path)
\end{itemize}

\section{Device Data Storage Architecture}
\subsection{Design goal}
Measurement output is first persisted on the device and uploaded after the run ends.
This avoids data loss during temporary network outages and supports delayed reporting.

\subsection{Local spool layout}
Agent persists run data under \texttt{DATA\_ROOT} (default \texttt{./data} in agent working directory):
\begin{verbatim}
DATA_ROOT/
  pending/<run_id>/
    context.json
    events.ndjson
    report.json
    artifacts/
      <command-id>-<timestamp>.log
      run-summary.json
  sent/<run_id>/
    ...same structure after successful upload...
\end{verbatim}

\subsection{Upload semantics}
\begin{itemize}
  \item After command execution, agent builds \texttt{Report}, writes it to \texttt{pending/<run\_id>/report.json}, then tries upload.
  \item Agent replays pending reports through \texttt{PublishReport}.
  \item On \texttt{ACCEPTED} or \texttt{DUPLICATE}, run is moved to \texttt{sent/<run\_id>/}.
  \item Sent runs are pruned by \texttt{SENT\_RETENTION\_DAYS} (default 14).
\end{itemize}

\subsection{Artifact references}
Current implementation stores artifact metadata in gRPC report and eLabFTW event metadata:
\begin{itemize}
  \item per-command log artifacts,
  \item \texttt{run-summary.json},
  \item URI format currently \texttt{spool://<run-id>/artifacts/<artifact-name>}.
\end{itemize}

This is a deferred-report architecture; binary upload to central object storage can be added later without changing core run lifecycle.

\section{Metrics Storage Pipeline (v3)}
\subsection{Goal}
Store Wi-Fi/BLE/CSI metrics in a time-series backend (Mimir) and visualize in Grafana without requiring Prometheus on devices.

\subsection{Pipeline}
\begin{verbatim}
Device Agent / Embedded Sender -> Monad Fleet HTTP ingest (/ingest/v1/metrics)
                                  + gRPC PublishReport summary metrics
                               -> Monad Fleet /metrics
                               -> Prometheus scrape + remote_write
                               -> Mimir
                               -> Grafana
\end{verbatim}

\subsection{Low-resource sender contract}
Hypothetical low-level boards (no OS, low storage) can send compact JSON payloads to Monad Fleet:
\begin{verbatim}
POST /ingest/v1/metrics
{
  "source": "embedded-hypothetical",
  "device_id": "lowlevel-board-01",
  "metrics": [
    {"name": "wifi_rssi_dbm", "value": -58.2},
    {"name": "ble_adv_count", "value": 81},
    {"name": "csi_frames_count", "value": 420}
  ]
}
\end{verbatim}

This allows telemetry forwarding to Mimir even when the sender cannot run Prometheus.

\subsection{CSI/BLE/Wi-Fi: metrics vs. raw artifacts}
Grafana Mimir is a time-series database for \textbf{numeric} samples, not for raw binary captures.
Therefore:
\begin{itemize}
  \item store raw CSI / BLE traces / PCAPs as \textbf{artifacts} on the device (or in object storage) and report only \textbf{references} via \texttt{ArtifactRef.uri},
  \item compute derived numeric metrics (counts, RSSI/SNR summaries, percentiles, error rates) and send them via \texttt{Event.metrics} and/or \texttt{Report.summary\_metrics}.
\end{itemize}

\subsection{Smoke test (v3) for end-to-end validation}
The repository includes a repeatable smoke test that resets only Fleet/metrics state, creates a dedicated smoke experiment in eLabFTW, runs one device cycle, and verifies metrics in Prometheus and Mimir:
\begin{verbatim}
scripts/smoke/v3_end_to_end_smoke.sh
\end{verbatim}

For manual resets (without running the smoke test), use:
\begin{verbatim}
scripts/reset/dev_reset.sh
\end{verbatim}

Behavior:
\begin{itemize}
  \item Does \textbf{not} modify existing experiments; it creates a new one tagged with \texttt{fleet}.
  \item Does \textbf{not} delete MySQL/eLabFTW user data; it only resets:
    \begin{itemize}
      \item \texttt{./data/fleet/state.json} and \texttt{./data/fleet/ingest-metrics.ndjson},
      \item Prometheus and Mimir local storage volumes.
    \end{itemize}
  \item Leaves the created smoke experiment in eLabFTW for inspection (unless cleanup is enabled).
\end{itemize}

Environment variables:
\begin{verbatim}
DEVICE_MODEL=02:42:ac:14:00:04     # model-device MAC
DEVICE_PI=2c:cf:67:80:f5:d9        # Raspberry Pi MAC (optional target)
RESET_STATE=true|false
RESET_METRICS=true|false
RESET_GRAFANA=true|false
RUN_PI=true|false
PI_HOST=monad-rpi5.local
DO_BUILD=true|false
CLEANUP=true|false
\end{verbatim}

Web endpoints after running compose:
\begin{itemize}
  \item eLabFTW: \texttt{https://localhost:8443}
  \item Grafana: \texttt{http://localhost:3000} (default \texttt{admin/admin})
  \item Prometheus: \texttt{http://localhost:9090}
\end{itemize}

\textbf{Note:} the reference agent can collect real Wi-Fi/BLE metrics on Linux when \texttt{EXECUTE\_POLICY=true} (using \texttt{iw} and \texttt{bluetoothctl} with fallbacks). In simulation mode and inside containers without radio access, these counters may be 0; the pipeline is still validated by the HTTP ingest example.

\subsection{PromQL examples}
Example PromQL queries (usable in Prometheus UI and Grafana when Mimir datasource is selected):
\begin{verbatim}
# Latest totals (per device):
monad_fleet_metric_value{device_id="02:42:ac:14:00:04",metric="wifi_ap_total"}
monad_fleet_metric_value{device_id="02:42:ac:14:00:04",metric="ble_adv_total"}
monad_fleet_metric_value{device_id="02:42:ac:14:00:04",metric="csi_frames_total"}

# Wi-Fi link quality / RSSI (real devices, Linux):
monad_fleet_metric_value{device_id="2c:cf:67:80:f5:d9",metric="wifi_avg_rssi_dbm"}
monad_fleet_metric_value{device_id="2c:cf:67:80:f5:d9",metric="wifi_link_quality"}
monad_fleet_metric_value{device_id="2c:cf:67:80:f5:d9",metric="wifi_connected"}

# BLE scan status (1 = scan command executed, even if 0 devices observed):
monad_fleet_metric_value{device_id="2c:cf:67:80:f5:d9",metric="ble_scan_ok"}

# Low-level sender example:
monad_fleet_metric_value{device_id="lowlevel-board-01"}

# Service counters:
monad_fleet_reports_total
monad_fleet_events_total
\end{verbatim}

\section{Policy Source and Selection}
\subsection{Where policy is read from}
Fleet policies are read from eLabFTW experiments tagged by \texttt{FLEET\_EXPERIMENT\_TAG} (default \texttt{fleet}).
\textbf{Categories are not used} for fleet logic; tagging and metadata are the only required eLabFTW features.

Policy should be stored in the experiment \textbf{metadata JSON editor} (not as file attachments), so the policy can be read and revised via API and remains easily auditable.

Default metadata keys checked in order:
\begin{verbatim}
fleet.policy, policy, fleet_policy, policy_json
\end{verbatim}

\textbf{Tag format note:} ensure \texttt{fleet} is a distinct tag. When creating experiments via the eLabFTW REST API, prefer passing \texttt{tags} as an array of strings (so the API can filter by \texttt{tags[]=fleet} reliably).

\subsection{Selection logic}
\begin{itemize}
  \item candidate experiments are filtered by fleet tag,
  \item optional tag matching by prefix (\texttt{RESOURCE\_TAG\_PREFIX}, default \texttt{device:}),
  \item optional \texttt{target\_selector} filtering on \texttt{device\_ids}, \texttt{device\_types}, \texttt{locations},
  \item newest modified matching experiment with a parseable policy wins.
\end{itemize}

\subsection{Policy revision}
Server canonicalizes policy JSON and computes:
\begin{verbatim}
policy_id = sha256:<digest>
\end{verbatim}

\section{eLabFTW Mapping}
\subsection{Creating fleet experiments (manual workflow)}
\begin{enumerate}
  \item Create a new experiment in eLabFTW.
  \item Add the tag \texttt{fleet}. (This is the only required classifier; categories are ignored by Monad Fleet.)
  \item Open the \textbf{Metadata} JSON editor and store policy JSON under \texttt{fleet.policy}.
  \item (Optional) Add \texttt{device:<tag>} tags to the experiment and to the device resource item to enable tag-based matching (\texttt{RESOURCE\_TAG\_PREFIX}).
  \item Save. The newest modified matching experiment is selected for devices.
\end{enumerate}

Recommended policy placement in metadata:
\begin{verbatim}
{
  "fleet": {
    "policy": { ... }
  }
}
\end{verbatim}

\subsection{Device resource items}
On first contact, server creates a resource item named \texttt{Device <agent\_id>} and updates its metadata on each heartbeat.

Current implementation enforces bookable defaults for device resources:
\begin{itemize}
  \item \texttt{is\_bookable=1}
  \item \texttt{book\_max\_minutes=180} (configurable)
  \item \texttt{book\_can\_overlap=1} (configurable)
  \item \texttt{book\_is\_cancellable=1} (configurable)
\end{itemize}

\subsection{Scheduler events}
Run data is stored in eLabFTW scheduler events linked to device resource items.

Run aggregation key:
\begin{verbatim}
experiment_id | policy_revision | device_id
\end{verbatim}

Event metadata includes:
\begin{itemize}
  \item current status (\texttt{RUNNING}, \texttt{COMPLETED}, etc.),
  \item event history,
  \item summary metrics (\texttt{events\_total}, \texttt{commands\_failed}, artifacts count),
  \item last event payload.
\end{itemize}

Status semantics:
\begin{itemize}
  \item status is treated as monotonic for a run: terminal states (\texttt{COMPLETED}, \texttt{PARTIAL}, \texttt{FAILED}) must not be downgraded by later artifact uploads.
\end{itemize}

\section{Wi-Fi Device Interoperability Guide}
\subsection{Minimal requirements}
\begin{itemize}
  \item Wi-Fi connectivity to Monad Fleet service host and port.
  \item Generated client stubs from \texttt{fleet\_gateway\_v2.proto}.
  \item Stable \texttt{agent\_id} (recommended: lower-case MAC or UUID).
\end{itemize}

\subsection{Wi-Fi-only mode recommendation}
For Wi-Fi-only devices, use:
\begin{itemize}
  \item \texttt{requested\_mode = RF\_SHARING}
  \item \texttt{control\_plane\_iface = wlan0} (or actual Wi-Fi iface)
\end{itemize}

\subsection{Reference environment variables}
\begin{verbatim}
FLEET_MANAGER_HOST=<server_host>
FLEET_MANAGER_PORT=50060
AGENT_ID=<stable_device_id>
CONTROL_PLANE_MODE=RF_SHARING
CONTROL_PLANE_IFACE=wlan0
WIFI_SCAN_IFACE=wlan0   # optional; defaults to CONTROL_PLANE_IFACE
MAX_SYNC_CYCLES=0
EXECUTE_POLICY=false    # set true on real devices to execute typed commands and collect metrics
CAPABILITIES=csi,ble,wifi
DATA_ROOT=./data
SENT_RETENTION_DAYS=14
\end{verbatim}

\subsection{Raspberry Pi reference setup (Wi-Fi only)}
This repository includes helper scripts to deploy the reference v2 agent to a Raspberry Pi and run it as a service:
\begin{verbatim}
scripts/rpi/deploy_agent.sh
scripts/rpi/install_systemd_service.sh
scripts/rpi/smoke_test_agent.sh
\end{verbatim}

Recommended settings for a Wi-Fi-only Pi:
\begin{itemize}
  \item \texttt{CONTROL\_PLANE\_MODE=RF\_SHARING}
  \item \texttt{CONTROL\_PLANE\_IFACE=wlan0} (or the actual Wi-Fi iface)
  \item set \texttt{EXECUTE\_POLICY=true} to collect real Wi-Fi/BLE metrics (uses \texttt{iw} and \texttt{bluetoothctl} if available)
  \item \texttt{AGENT\_ID} derived from the Wi-Fi MAC (stable across reboots)
\end{itemize}

\section{Recommended Client Loop}
\begin{verbatim}
1) Hello(agent descriptor)
2) loop:
   a) GetAssignment(agent_id)
   b) if NO_WORK -> sleep(poll)
   c) GetPolicy(agent_id, experiment_id, last_policy_id)
   d) if NOT_MODIFIED -> sleep(poll)
   e) AckPrepared(...)
   f) execute command_groups/commands in order
   g) PublishReport(run_id, events, artifacts, summary)
   h) set last_policy_id on ACCEPTED or DUPLICATE
   i) sleep(poll)
\end{verbatim}

\section{Policy JSON Example (stored in eLabFTW metadata)}
\begin{verbatim}
{
  "fleet": {
    "policy": {
      "target_selector": {
        "device_ids": ["2c:cf:67:80:f5:d9"]
      },
      "range": {
        "from": "2026-02-17T19:41:31Z",
        "to":   "2026-02-17T21:42:31Z"
      },
      "command_groups": [
        {
          "id": "wifi-ble-csi-v3",
          "name": "WiFi BLE CSI v3",
          "failure_mode": "FAIL_FAST",
          "commands": [
            {
              "id": "wifi-scan",
              "type": "WIFI_SCAN",
              "timeout_s": 15
            },
            {
              "id": "ble-scan",
              "type": "BLE_SCAN",
              "timeout_s": 15
            },
            {
              "id": "csi-capture",
              "type": "CAPTURE_CSI",
              "timeout_s": 15
            }
          ]
        }
      ]
    }
  }
}
\end{verbatim}

\section{Implementation Notes}
\begin{itemize}
  \item \texttt{PublishEvents} is optional and disabled by default in RF-sharing deployments.
  \item If \texttt{Report.events} is empty, server synthesizes one \texttt{RUN\_FINISHED} event from report summary.
  \item If \texttt{Event.event\_id} is empty, server synthesizes a stable-ish id from (\texttt{run\_id}, \texttt{command\_id}, \texttt{type}, \texttt{timestamp}).
  \item \texttt{RetryPolicy.max\_attempts} in v2 Policy is derived from JSON retries and may be transformed by server normalization.
  \item v3 smoke flow emits summary metrics (\texttt{wifi\_ap\_total}, \texttt{ble\_adv\_total}, \texttt{csi\_frames\_total}) for time-series ingestion.
  \item To forward metrics to a different system, change Prometheus \texttt{remote\_write} configuration (e.g., \texttt{observability/prometheus/prometheus.yml}); the agent gRPC contract remains unchanged.
\end{itemize}

\section{Versioning and Compatibility}
\begin{itemize}
  \item Preferred interface for new device implementations: \texttt{fleet.v2}.
  \item Legacy \texttt{fleet.v1} remains available for backward compatibility.
  \item New clients should treat unknown fields and enum values as forward-compatible additions.
\end{itemize}

\section{Appendix: Current Deployment Defaults}
From current compose/runtime configuration:
\begin{itemize}
  \item gRPC listen: \texttt{0.0.0.0:50060}
  \item HTTP sidecar listen: \texttt{0.0.0.0:9108}
  \item eLabFTW API base: \texttt{https://web/api/v2}
  \item Fleet experiment tag: \texttt{fleet}
  \item Policy keys: \texttt{fleet.policy,policy,fleet\_policy,policy\_json}
  \item Live events: disabled
  \item Device spool root default: \texttt{./data}
  \item Sent-run retention default: \texttt{14 days}
  \item Prometheus: \texttt{http://localhost:9090}
  \item Mimir: \texttt{http://localhost:9009}
  \item Grafana: \texttt{http://localhost:3000} (default \texttt{admin/admin})
\end{itemize}

\end{document}
