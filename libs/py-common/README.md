# py-common

Reserved location for shared Python utilities used across multiple apps.

Current migration phase keeps code in place and introduces this directory as the canonical shared-library target for future refactors.
